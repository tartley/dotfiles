# First, ignore everything by default.
*
# Directories whose contents I will usually want to track.
# Unignore the directory itself before the files within it.
!/.bazaar/
!/.bazaar/**
!/.config/
!/.config/conky/
!/.config/conky/**
!/.config/nvim/
!/.config/nvim/**
!/.rgignore
/.config/nvim/.netrwhist
!/bin/
!/bin/**

