#!/usr/bin/env bash
ctags -R \
    --languages=python \
    --exclude=.bzr \
    --exclude=.eggs \
    --exclude=.env \
    --exclude=.git \
    --exclude=.tox \
    --exclude=.venv \
    --exclude=env \
    "$@"

