#!/usr/bin/env bash

# edit my TODO file in a new window
# -t values are args for gnome-terminal, and select a slightly enlarged vector font.
# args after the filename to edit are for nvim itself.
nw -t --profile=Pop -t --zoom=1.2 ~/TODO.md -c "set lines=45 | set nonumber | set columns=80"

